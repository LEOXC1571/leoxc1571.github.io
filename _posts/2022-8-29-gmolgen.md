---
layout: post
title: Molecule Generation for Drug Design：a Graph Leadning Perspective
categories: [Molecule, GNN]
description: 基于图的分子生成综述
keywords: 综述, 分子生成
---

## 摘要
从机器学习的应用角度，一个新兴领域就是利用其辅助药物的生成与发现。本文提供了对时下最优的基于图的分子设计与发现方法的综述。本文将这些方法分为3类：一步生成、基于子图生成与节点依次生成。同时，本文也提供了一些具有代表性的公共数据集与常用的评价指标。最后，本文探讨了该领域未来研究面临的挑战。

## 序言
- 基于SMILES序列的生成模型十分丰富，但是基于3D分子的生成模型研究较少，现有的基于3D的方法包括G-SchNet和G-SphereNet。
- 基于SMILES的分子生成模型无法保证生成分子的100%有效性（除非添加复杂限制条件）。相对而言，基于2D的分子生成模型能够很容易的保证生成分子的有效性。
- 与其他综述不同的是，本文着重关注针对药品研发的分子图生成模型。

## 生成与优化策略
### 一步生成
- VGAE基于变分自编码器构建无监督学习模型，通过学习输入图的隐向量生成新的分子图。但其只能够从一个输入分子图学习，相应的，GraphVAE能够从一系列图学习，其中他使用GCN学习节点表征。RVAE同样是基于AE的模型，其提出一个正则化框架用于保证分子有效性。此外还有用于大图生成的MPGVAE。
- MolGAN提出了一个运用GAN生成小分子图的隐式生成模型。其首先根据标准正态分布生成一个隐向量，最后运用一个MLP一步生成分子图。
- GraphNVP是首个基于可逆归一化流模型的分子生成模型。其运用去量化方法将离散邻接矩阵和节点标签矩阵转化为连续节点表征，并运用耦合层获得隐向量表征。在从一个先验分布进行采样获得隐向量后，GraphNVP运用两步生成分子。第一步是运用隐向量生成图结构，第二部是根据隐向量与图结构生成节点表征。

### 基于子图生成
- 许多模型运用分子子结构用于分子图生成。JT-VAE首先将分子图$G$分解为节点树$\mathcal{T}_G$，其中每个节点分子的一个子结构。JT-VAE随后将树与图编码至隐向量空间，得到$z_{\mathcal{T}}$与$z_{G}$。在解码阶段，模型根据$z_{\mathcal{T}}$重建节点树，并随后根据重建树与$z_{G}$生成分子图结构。HierVAE是聆听一个基于AE的模型，其提出了运用更大且复杂的分子基团作为基础，并在面对大分子的时候获得更高的重建准确性。与JT-VAE不同的是，HierVAE通过一个层次图表现。其包括三个不同的层，包括原子层，附着层与基团层。编码器生成每个节点在对应三层的表征，而解码器从粗到细重建分子。

### 节点依次生成
- CGVAE基于自编码器，将GGNNs用作编码、解码器。其运用GGNN将输入图$G$中的节点编码成服从对角正态分布的隐向量。而解码器将带隐向量的节点初始化，并依据两个决策函数（注意力与扩展）依次生成节点之间的边。


### 讨论
不同层面的生成方法在不同应用中具备不同的优势。总的来说，从节点层面的精细操作是灵活的，但其面对高阶结构建模时可能不够有效。基于子图的方法能够编辑分子的子结构，这能够对特定任务和反应提供效果。一步生成的范式效率很好但其面对增量生成和优化常常缺乏灵活性。

## 数据集与评价指标
### 数据集
- 数据集包括QM9（由CHONF组成的稳定有机小分子），GDB-17（最多17个原子的有机小分子的枚举），ZINC15（商用成分），ChEMBL（具备药学性质的生物活性分子），PubChemQC（具备基于密度泛函理论估计得到的量子化学性质的成分），DrugBank（FDA审查通过的药物和其他公开药物）。

### 评价指标
- 分子生成与优化采用两类不同的评价指标。
- 分子生成从统计角度评价生成分子的总体质量，包括有效性，创新性，多样性，唯一性与重建性。
- 针对分子优化，采用的评价指标包括$QED$（类药性期望），$SA$（综合合成性），$logP$辛醛水分配系数。

## 挑战与未来方向
- **聚合物**：现有工作主要聚焦与小分子设计，但当这些模型遇到设计大分子（如聚合物）的任务时，效果大打折扣。其有效性的下降可能是由于：许多生成步骤要求简历对大分子的全局认知，以及迭代步骤中梯度的相关挑战。
- **3D药物建模**：生成三维分子仍是一个需要研究的方向。较基于SMILES的图生成模型，维度的扩展能够显著提升分子的探索空间，同时也带来训练的困难。但是生成三维分子是十分具有意义且必要的，因为三维坐标对于具体属性的准确量化预测十分重要。
- **目标探索**：在这特指对特定疾病生成可能相关的药物。化学的空间是十分广阔的，但是具备特定理想性质的分子是十分有限的。核心的目标就从有潜力的化学空间中筛选可能的成分，并针对特定生物目标进行筛选。寻找一个目标小分子并与目标蛋白质有良好绑定效果是一个现实的挑战。这同样被成为基于结构的分子发现。